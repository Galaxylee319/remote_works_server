# 开源文件服务方案调研（GitHub）

> 调研时间：2026-09-10 ｜ 目的：为自研的 remote_works 文件服务器（FastAPI + 自建 UI）寻找可借鉴点
> 数据来源：GitHub REST API（`api.github.com`）实时查询

## 一、候选项目对比

| 项目 | ★ | 语言 | 许可 | 最近更新 | 特点 | 对我们的价值 |
|---|---:|---|---|---|---|---|
| [9001/copyparty](https://github.com/9001/copyparty) | 46.6k | Python | MIT | 2026-09-08 | 单文件可跑；缩略图网格、断点续传、去重、WebDAV、RSS/OPDS、日志实时 tail、Markdown 查看/编辑、播放器 | ⭐ **最佳借鉴对象**（同为 Python，功能面最全） |
| [filebrowser/filebrowser](https://github.com/filebrowser/filebrowser) | 36.0k | Go | Apache-2.0 | ⚠️ **2026-09-01 归档** | 经典文件管理器 UI | ⚠️ **不建议采用**：已停止维护，官方明示两类漏洞不再修（命令执行、JWT 会话无法吊销）；仅可借鉴其 UI 布局 |
| [squidfunk/mkdocs-material](https://github.com/squidfunk/mkdocs-material) | 27.4k | Python | MIT | 2026-08-30 | 文档站主题：搜索、导航树、TOC、多语言、代码高亮 | 若将来把报告整理成"文档站"可选 |
| [mickael-kerjean/filestash](https://github.com/mickael-kerjean/filestash) | 14.6k | Go | AGPL-3.0 | 2026-09-10 | 多存储后端（S3/FTP/SFTP…）统一前端 | 后端抽象思路；AGPL 需注意 |
| [jackyzha0/quartz](https://github.com/jackyzha0/quartz) | 13.2k | TypeScript | MIT | 2026-09-06 | Markdown → 静态站：全文搜索、双链、知识图谱、LaTeX | 「研究笔记站」路线的代表 |
| [sigoden/dufs](https://github.com/sigoden/dufs) | 10.7k | Rust | Apache-2.0 | 2026-06-29 | 轻量静态服务 + 上传 + **搜索** | 极简替代品 |
| [Pagefind/pagefind](https://github.com/Pagefind/pagefind) | 5.4k | Rust | MIT | 2026-09-10 | 面向静态站的**低带宽全文检索** | ⭐ 本次检索功能的思路来源 |
| [filegator/filegator](https://github.com/filegator/filegator) | 3.1k | PHP | MIT | 2026-09-07 | 多用户文件管理 | 多用户/权限模型参考 |

**结论**：主流那款（filebrowser）已归档且有未修漏洞，**不宜迁移**；copyparty 是当前最活跃、功能最全的同类，且是 Python 实现，**最值得我们借鉴**。

## 二、本次已落地的借鉴（3 项）

| # | 功能 | 借鉴自 | 实现 |
|---|---|---|---|
| 1 | **正文全文检索** | Pagefind 的静态检索思路 / copyparty 的 file-search | `app/file_browser.py: search_content()`：按文本扩展名白名单扫描，大小写不敏感子串匹配（中文无需分词），按 `(mtime,size)` 做内存缓存；结果按命中数排序，返回**带行号与上下文的片段**；`/search?mode=content` 与 `/api/search?mode=content` |
| 2 | **缩略图网格视图** | filebrowser 的 grid / copyparty 的 thumbnails | 新增 `/thumb/{path}?w=` 端点：Pillow 生成 JPEG 并落盘缓存（键含 mtime/size/宽度）；`browse.html` 增加「列表 / 网格」切换（localStorage 记忆），图片走缩略图、`loading="lazy"`，失败自动回退为图标 |
| 3 | **检索结果高亮与片段** | Pagefind 的结果呈现 | `search.html` 增加模式切换（按文件名 / 按正文内容）、命中计数徽标、`<mark>` 高亮、行号前缀 |

**实测**：正文检索「Type-I」0.18 s 返回 40 处命中；中文「退化机制」16 个文件命中并高亮；缩略图 1.1 MB PNG → 7.9 KB（320px），首次 0.23 s、缓存命中 0.003 s。

## 三、建议但本次未做（按性价比排序）

1. **导航树侧栏（navpane）**——copyparty 支持面包屑与树形导航切换，长路径下更省点击（中等改动）
2. **日志实时 tail**——copyparty 的 textfile viewer 支持流式追踪；我们有 `typeI_logs/`（655 个日志），浏览器里直接跟日志会很有用（中等）
3. **RSS / OPDS 订阅**——「最近更新」的 RSS 订阅（改动小）；OPDS 便于电子书阅读器拉取 PDF（改动小）
4. **目录体积/数量统计**——列表页显示子目录条目数与总大小（改动小）
5. **批量选择与打包下载**——copyparty 支持多选打包；当前只能整目录打包（中等）
6. **文档站路线**——用 mkdocs-material 或 Quartz 把 `docs/`+`phases/`+`papers/` 生成带搜索的静态站点，与现有文件浏览并存（较大，但检索/导航体验最好）
7. **WebDAV / SFTP 只读访问**——便于用本地工具直接读（中等；copyparty/dufs 均支持）

## 四、注意事项

- 本服务定位是**只读浏览**（安全设计），copyparty/filebrowser 的上传、重命名、分享等写操作类功能**不适合照搬**，需保持只读。
- 若考虑引入 WebDAV/SFTP，须先评估鉴权与暴露面（当前 8088 暴露在局域网 + Tailscale，见运维报告）。
